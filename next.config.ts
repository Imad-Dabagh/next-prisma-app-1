import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 't4.ftcdn.net', 'i.pinimg.com','i.pravatar.cc','randomuser.me'],
  },
};

export default nextConfig;
